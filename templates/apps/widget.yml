app:
  models:
    company:
      id: int
      name: string
      description: string
      users: list[str]

    user:
      id: int
      email: string
      roles: list[str]

    role:
      id: int
      name: string
      description: string
      products: list[string]

    transcription_request:
      id: int
      files: list[string]

    transcription:
      id: int
      type: string
      status: string
      files_path: string

    transcription_result:
      id: int
      type: string
      status: string
      files: list[string]

app:
  models:
    company:
      id: string
      name: string
      description: string
      users: list[str]

    user:
      id: string
      email: string
      roles: list[str]

    role:
      id: string
      name: string
      description: string
      products: list[string]

    transcription_request:
      id: string
      files: list[string]

    transcription:
      id: string
      type: string
      status: string
      files_path: string

    transcription_result:
      id: string
      type: string
      status: string
      files: list[string]
